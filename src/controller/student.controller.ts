import { Request, Response } from "express";




export const getAllStudent = (req: Request, res: Response ) => {
    
}

export const addStudent = (req: Request, res: Response ) => {
    
}

export const getByStudentId = (req: Request, res: Response ) => {
    
}